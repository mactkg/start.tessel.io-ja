- var thismod = "ble"
- var pkg = "ble-ble113a"
- var doThis = "Set a Bluetooth Low Energy device to advertising and see if Tessel can find it!"
- var bonus = "<br/><br/><b>Bonus:</b> Change the code to print out only the IP address of discovered peripherals."

- var i = 1
- var moduleDocs = moduleDocs_filepath + pkg
- var moreInfo = moreInfo1 + thismod.toUpperCase() + moreInfo2 + moduleDocs + moreInfo3

//- +see_video("http://youtu.be/JLZzLyH9jc4")
+make_directory(i, thismod)
- i++
+normal_row(i, modulesPlugged_filepath +  thismod + ".jpeg", plugIn.replace('#{module}', thismod.toUpperCase()).replace('#{port}', 'A'))
- i++
+normal_row(i, moduleCorners_filepath + thismod + ".jpg", install.replace('#{package}', pkg))
- i++
+displayCode(i, code.code, thismod)
- i++
+normal_row(i, moduleGifs_filepath + thismod + ".gif", push.replace('#{module}', thismod) + doThis + bonus + moreInfo)
- i++
+seeForums(i, thismod, thismod.toUpperCase())
